{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": { "npmArgs": "--legacy-peer-deps" }
      }
    ],
    "routes": [
      { "handle": "filesystem" },
      { "src": "/.*", "dest": "/index.html" }
    ]
  }
